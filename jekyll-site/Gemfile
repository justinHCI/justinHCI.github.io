require 'csv'
source 'https://rubygems.org'

# filepath: c:\Users\jedwards23\OneDrive - Oulun yliopisto\Code\Websites\justinHCI.github.io (backup)\jekyll-site\Gemfile
gem 'csv'
gem 'base64'
gem'bigdecimal'
gem 'minima', '~> 2.5'
gem 'wdm', '>= 0.1.0' if Gem.win_platform? 
gem 'logger'
gem 'jekyll', '~> 4.2.0'
gem 'webrick'  # Required for running Jekyll on Ruby 3.0 and later
gem 'jekyll-feed', '~> 0.15' # Moved outside development group


group :development do
  # Add any gems specifically for development here if needed
end

# If you plan to use a specific theme, add it here, e.g.:
# gem "minima", "~> 2.5" 